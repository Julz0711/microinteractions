import { RoomGrid } from "./RoomGrid";

export const DashboardGrid = () => {
  return (
    <div className="h-[700px] w-full flex flex-col items-center justify-center">
      <RoomGrid />
    </div>
  );
};
